# Derivados regeneráveis — nunca versionar (regenerados a partir da canonica.md)
**/saidas/

# Temporários e backups
*.tmp
*.bak
*.swp
*~

# Lixo de SO
.DS_Store
Thumbs.db
desktop.ini

# Editores
.idea/
*.code-workspace

# Python / venv (ferramentas)
.venv/
__pycache__/
*.pyc

# Mídia bruta no lake — pesada, não versionar (só os .md transcritos entram)
lake/**/*.mp4
lake/**/*.mkv
lake/**/*.mov
lake/**/*.avi
lake/**/*.mp3
lake/**/*.wav
lake/**/*.m4a
